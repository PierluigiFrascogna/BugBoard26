spring:
  application:
    name: users
  datasource:
    url: jdbc:postgresql://db.hwcirxykymtcakpdevzx.supabase.co:5432/postgres?sslmode=require
    username: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
server:
  port: 8081
